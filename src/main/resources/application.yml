spring:
  application:
    name: ai-agent-toy-project

server:
  port: 8080

# 이 프로젝트는 외부 인프라(Redis/DB/Kafka 등)를 전혀 사용하지 않는다.
# 모든 데이터와 LLM 은 Mock 으로 동작한다.
logging:
  level:
    com.example.aiagent: DEBUG
